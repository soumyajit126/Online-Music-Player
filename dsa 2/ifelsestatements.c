#include<stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    if (n==0){

    }
    else{
        for(int i=1;i<=n;i++);
    }
}//log is just the 2^3=8 and log^2(8)=3 are just the way of writing